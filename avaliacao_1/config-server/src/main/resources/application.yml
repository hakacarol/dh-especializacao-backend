server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/hakacarol/dh-especializacao-backend
          default-label: main
          search-paths:
            - 'cloud-config-avaliacao-1*'